export interface PaginationData {
  currentPage: number;
  itemsCount: number;
  pagesCount: number;
  nextPage: number;
  hasNextPage: boolean;
  previousPage: number;
  hasPreviousPage: boolean;
  itemsPerPage: number;
}
